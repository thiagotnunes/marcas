<p>
  <h1><%= t('user.title.user') %></h1>
</p>

<p>
  <b><%= t('helpers.label.user.username') %></b><br>
  <%= @user.username %>
</p>

<p>
  <b><%= t('helpers.label.user.email') %></b><br>
  <%= @user.email %>
</p>

<div class="form-actions">
  <% if can? :edit_password, @user %>
    <%= link_to t('button.change_password'), edit_password_path(@user), :id => "change_password", :class => 'btn'  %>
  <% end %>
  <%= link_to t('button.back'), users_path, :class => 'btn'  %>
</div>
